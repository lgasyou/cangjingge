spring:
  profiles:
    active: "@activatedProperties@"

  application:
    name: authorization

  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"

  jpa:
    generate-ddl: true

eureka:
  instance:
    instance-id: authorization
